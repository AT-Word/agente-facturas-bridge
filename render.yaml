services:
  - type: web
    name: agente-facturas-bridge
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: APPS_SCRIPT_URL
        sync: false
